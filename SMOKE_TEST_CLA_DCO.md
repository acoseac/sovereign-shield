# CLA/DCO smoke test — DO NOT MERGE

Throwaway pull request to confirm the `cla (signed)` and `dco (sign-off present)`
gates actually execute on a real PR (the CLA workflow had never run before it landed
on `main`). This file and its branch are deleted once the checks are observed.
